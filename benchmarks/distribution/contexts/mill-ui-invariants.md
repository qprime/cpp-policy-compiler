# mill_ui C++ Invariants

Dimensions are millimeters. Types establish their invariants at construction. The FFI
boundary validates external values and converts failures before they cross languages.
RemovalIntent describes what to machine before planners decide how. Frozen semantic
values are replaced rather than mutated, and native changes retain warning-clean C++20
builds plus adjacent tests.
